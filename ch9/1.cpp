#include <array>
#include <vector>
#include <iostream>
using std::vector;
int main()
{
    // std::array<int,2> aa={0,1};
    // std::array<int,2> ab={2,3};
    // std::array<int,2>::iterator aai = aa.begin();
    // std::array<int,2>::iterator abi = ab.begin();
    // std::swap(aa,ab);

    // std::vector<int> va={0,1};
    // std::vector<int> vb={2,3};
    // std::vector<int>::iterator vai = va.begin();
    // std::vector<int>::iterator vbi = vb.begin();
    // std::swap(va,vb);

    // vector<int> ivec;
    // std::cout << ivec.size() << " " <<ivec.capacity()<<std::endl;
    // ivec.push_back(1);
    // ivec.push_back(1);
    // ivec.push_back(1);
    // ivec.reserve(50);
    // std::cout << ivec.size() << " " <<ivec.capacity()<<std::endl;




    return 0;

}